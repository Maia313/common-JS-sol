#### Everything is a todo

##### Steps when planning

```js
//step 1
// It should have a place to store data. - ex. Array, array within an object 

//step 2
// It should have a function to display data.  

//step 3  CREATE
// It should have a function to add data.

//step 4  UPDATE
// It should have a function to edit data.

//step 5  DELETE
// It should have a function to remove data.

//step 6  
// It should have a function to toggle data.


```
